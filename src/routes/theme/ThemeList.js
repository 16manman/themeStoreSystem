import React from 'react';
import { connect } from 'dva';
import { styles } from './index.less';

class ThemeList extends React.Component {
	state = {
		
	}
	componentDidMount(){
		
	}
}
export default connect((theme)=>{{theme}})